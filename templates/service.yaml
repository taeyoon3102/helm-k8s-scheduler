apiVersion: v1
kind: Service
metadata:
  name: {{ template "tytest.fullname" . }}
  labels:
    app: {{ template "tytest.name" . }}
    chart: {{ template "tytest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tytest.name" . }}
    release: {{ .Release.Name }}
